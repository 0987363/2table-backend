address: :9080
database:
  type: local # 或 mongodb
  address: ./data
authentication:
  secret: AES256-Druid-heifeng-huteng-1234
log:
  level: info

storage:
  type: local # 或 s3
  local_config:
    path: /data/file # 本地模式使用

  #s3_config:
   # bucket: "my-app-bucket"
   # region: "us-west-1"
   # endpoint: "s3.amazonaws.com"
